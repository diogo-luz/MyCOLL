@page "/paises/delete"
@using Microsoft.EntityFrameworkCore
@using MyCOLL.Data.Entities
@using MyCOLL.Data.Data
@inject IDbContextFactory<ApplicationDbContext> DbFactory
@inject NavigationManager NavigationManager

<PageTitle>Eliminar País</PageTitle>

<div class="container-fluid">
    @if (pais is null) {
        <div class="d-flex justify-content-center align-items-center" style="min-height: 400px;">
            <div class="spinner-border text-danger" role="status">
                <span class="visually-hidden">A carregar...</span>
            </div>
        </div>
    } else {
        <!-- Header -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h1 class="mb-0 text-danger">
                    <i class="bi bi-exclamation-triangle"></i> Eliminar País
                </h1>
                <p class="text-muted mb-0">Esta ação não pode ser revertida</p>
            </div>
            <a href="/paises" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left"></i> Voltar
            </a>
        </div>

        <div class="row justify-content-center">
            <div class="col-lg-5">
                <div class="card border-danger shadow-sm">
                    <div class="card-header bg-danger text-white">
                        <h5 class="mb-0"><i class="bi bi-trash"></i> Confirmar Eliminação</h5>
                    </div>
                    <div class="card-body text-center">
                        <div class="alert alert-warning">
                            <i class="bi bi-exclamation-triangle"></i>
                            <strong>Atenção!</strong> Tem a certeza que pretende eliminar este país?
                        </div>

                        @if (pais.Bandeira is not null && pais.Bandeira.Length > 0) {
                            <img src="@($"data:image/png;base64,{Convert.ToBase64String(pais.Bandeira)}")"
                                 class="border mb-3" style="max-height: 80px;"/>
                        } else {
                            <div class="bg-light rounded d-flex align-items-center justify-content-center mx-auto mb-3" style="height: 80px; width: 80px;">
                                <i class="bi bi-flag text-muted fs-3"></i>
                            </div>
                        }

                        <h4>@pais.Nome</h4>
                        <span class="badge bg-secondary">Ordem: @pais.Ordem</span>
                    </div>
                    <div class="card-footer bg-light">
                        <EditForm method="post" Model="pais" OnValidSubmit="DeletePais" FormName="delete">
                            <div class="d-flex gap-2 justify-content-center">
                                <button type="submit" class="btn btn-danger">
                                    <i class="bi bi-trash"></i> Sim, Eliminar
                                </button>
                                <a href="/paises" class="btn btn-outline-secondary">
                                    <i class="bi bi-x-lg"></i> Cancelar
                                </a>
                            </div>
                        </EditForm>
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@code {
    private Pais? pais;

    [SupplyParameterFromQuery] private int Id { get; set; }

    protected override async Task OnInitializedAsync() {
        using var context = DbFactory.CreateDbContext();
        pais = await context.Paises.FirstOrDefaultAsync(m => m.Id == Id);

        if (pais is null) {
            NavigationManager.NavigateTo("notfound");
        }
    }

    private async Task DeletePais() {
        using var context = DbFactory.CreateDbContext();
        context.Paises.Remove(pais!);
        await context.SaveChangesAsync();
        NavigationManager.NavigateTo("/paises");
    }

}
